@model IEnumerable<AirUdC.GUI.Models.Parameters.MultimediaTypeModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MultimediaTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MultimediaTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MultimediaTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MultimediaTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MultimediaTypeId })
        </td>
    </tr>
}

</table>
